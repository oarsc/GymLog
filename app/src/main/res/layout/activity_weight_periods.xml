<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        app:title="@string/title_list_weight_period"
        app:menu="@menu/weight_periods_menu"
        app:navigationIcon="@drawable/ic_arrow_back_24dp" />

    <org.oar.gymlog.ui.common.components.listView.SimpleListView
        android:id="@+id/exercisesList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?actionBarSize"
        tools:context=".ui.weight.list.WeightPeriodsActivity"
        tools:listitem="@layout/listitem_weight_period" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>